<% document ||= @document %>
<%= form_for(document) do |f| %>
  <%= f.file_field :file, class: 'special', value: 'Auswaehlen' %>
  <%= f.hidden_field :document_type %>
  <%= f.submit value: 'Hochladen'%>
<% end %>
